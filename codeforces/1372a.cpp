#include <cstdio>

int main() {
    int t;
    scanf("%d", &t);
    while(t--) {
        int n;
        scanf("%d", &n);
        while(n--) {
            printf("1 ");
        }
        printf("\n");
    }
    return 0;
}